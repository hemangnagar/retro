"""retro — milestone retrospectives that carry transferable lessons across projects.

The reasoning/curation layer on top of learned-behavior (behavioral signal,
workspace-scoped). retro adds the ritual (milestone interviews), the scope
rule (project | stack | global), and the budget (a size-capped cross-project
playbook a new lesson must displace something to enter).
"""

__version__ = "0.1.0"
